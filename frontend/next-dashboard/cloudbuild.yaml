steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/next-dashboard'
      - '--build-arg'
      - 'NEXT_PUBLIC_API_GATEWAY=${_NEXT_PUBLIC_API_GATEWAY}'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/next-dashboard'
